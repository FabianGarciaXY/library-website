extends ../layout

block content

    h1= title

    ul.content-ul-list
        each book in list_books
            li
                a(href=book.url) #{book.title}
                |  (#{book.author.name})

        else
            li There are no books.